#include <iostream>
#include <cstdlib> // для system
#include <conio.h>

#define _USE_MATH_DEFINES
#include <math.h>

using namespace std;
/*

float maxx(float a, float b)
{
	if a>b
		return a
	if a<b
		return b
}



float foo_1(float p, float t)
{
	return sqrt(p+t*t)
}

float foo_2(float k, float p)
{
	return p + k;
}
*/



int main()
{
	setlocale(LC_ALL, "RUS");
	
	//y = pow(atan(x), 2);
	
	foo(345);

	_getch();
}